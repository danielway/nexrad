[package]
description = "Download and decode functions for NEXRAD radar data."
authors = ["Daniel Way <contact@danieldway.com>"]
name = "nexrad"
version = "0.0.1"
license = "MIT"
edition = "2021"
repository = "https://github.com/danielway/nexrad"
exclude = [".github"]

[[example]]
name = "download"
path = "examples/download.rs"
required-features = ["download"]

[features]
download = ["dep:aws-smithy-http", "dep:aws-sig-auth", "dep:aws-config", "dep:aws-sdk-s3"]

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
bincode = "1"
bzip2 = "0.4"
aws-smithy-http = { git = "https://github.com/awslabs/aws-sdk-rust", branch = "main", optional = true }
aws-sig-auth = { git = "https://github.com/awslabs/aws-sdk-rust", branch = "main", optional = true }
aws-config = { git = "https://github.com/awslabs/aws-sdk-rust", branch = "main", optional = true }
aws-sdk-s3 = { git = "https://github.com/awslabs/aws-sdk-rust", branch = "main", optional = true }

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
femtovg = "0.7.1"